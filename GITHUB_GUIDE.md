# 🚀 دليل ربط المشروع مع GitHub

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من:
- ✅ تثبيت Git على جهازك
- ✅ إنشاء حساب على GitHub
- ✅ إنشاء repository جديد: https://github.com/bndrjj/dash

---

## 🔧 خطوات الإعداد والربط

### 1️⃣ إعداد Git على جهازك

```bash
# تحقق من تثبيت Git
git --version

# إذا لم يكن مثبتاً:
# Windows: حمّل من https://git-scm.com
# Mac: brew install git
# Linux: sudo apt install git
```

### 2️⃣ إعداد معلوماتك في Git

```bash
git config --global user.name "اسمك"
git config --global user.email "your.email@example.com"
```

### 3️⃣ تهيئة المشروع المحلي

```bash
# انتقل إلى مجلد المشروع
cd /path/to/your/project

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# أول commit
git commit -m "Initial commit: لوحة التحكم التفاعلية"
```

### 4️⃣ ربط المشروع بـ GitHub

```bash
# ربط المستودع المحلي بـ GitHub
git remote add origin https://github.com/bndrjj/dash.git

# أو إذا كنت تستخدم SSH:
# git remote add origin git@github.com:bndrjj/dash.git

# التحقق من الربط
git remote -v
```

### 5️⃣ رفع الملفات إلى GitHub

```bash
# رفع الملفات للفرع الرئيسي
git branch -M main
git push -u origin main
```

---

## 📤 سير العمل اليومي

### إضافة تعديلات جديدة:

```bash
# 1. تعديل الملفات (مثل data.json)

# 2. إضافة التعديلات
git add .

# 3. عمل commit مع رسالة وصفية
git commit -m "تحديث: إضافة إحصائيات جديدة"

# 4. رفع التعديلات
git push origin main
```

### سحب آخر التحديثات:

```bash
# سحب التحديثات من GitHub
git pull origin main
```

---

## 🌐 تفعيل GitHub Pages

لنشر لوحة التحكم على الإنترنت مجاناً:

### الطريقة الأولى - من إعدادات المستودع:

1. اذهب إلى المستودع: https://github.com/bndrjj/dash
2. اضغط على **Settings**
3. من القائمة الجانبية اختر **Pages**
4. في قسم **Source** اختر:
   - Branch: `main`
   - Folder: `/ (root)`
5. اضغط **Save**
6. انتظر دقيقة ثم سيظهر رابط موقعك: `https://bndrjj.github.io/dash`

### الطريقة الثانية - من سطر الأوامر:

```bash
# إنشاء فرع gh-pages
git checkout -b gh-pages

# رفع الفرع
git push origin gh-pages

# العودة للفرع الرئيسي
git checkout main
```

---

## 🔐 أفضل الممارسات الأمنية

### استخدام SSH بدلاً من HTTPS:

```bash
# توليد مفتاح SSH
ssh-keygen -t ed25519 -C "your.email@example.com"

# نسخ المفتاح العام
cat ~/.ssh/id_ed25519.pub

# أضف المفتاح في GitHub:
# Settings → SSH and GPG keys → New SSH key
```

### حماية الملفات الحساسة:

أضف الملفات الحساسة في `.gitignore`:

```
# ملفات الإعدادات
config.json
.env

# مفاتيح API
api_keys.txt
secrets/
```

---

## 📊 إدارة الفروع (Branches)

### إنشاء فرع جديد للتطوير:

```bash
# إنشاء والانتقال لفرع جديد
git checkout -b feature/new-design

# عمل التعديلات...

# رفع الفرع الجديد
git push origin feature/new-design
```

### دمج الفروع:

```bash
# العودة للفرع الرئيسي
git checkout main

# دمج الفرع
git merge feature/new-design

# رفع التحديثات
git push origin main
```

---

## 🔄 التعاون مع الآخرين

### إضافة متعاونين:

1. اذهب إلى المستودع
2. **Settings** → **Collaborators**
3. **Add people** → أدخل اسم المستخدم

### مراجعة Pull Requests:

1. الآخرون يعملون Fork للمستودع
2. يرسلون Pull Request
3. راجع التغييرات من تبويب **Pull requests**
4. اعتمد أو ارفض التغييرات

---

## 🛠️ أوامر Git المفيدة

```bash
# عرض حالة المشروع
git status

# عرض سجل التعديلات
git log --oneline

# التراجع عن آخر commit (بدون حذف التغييرات)
git reset --soft HEAD~1

# التراجع عن التعديلات غير المحفوظة
git checkout -- .

# عرض الفروع
git branch -a

# حذف فرع محلي
git branch -d branch-name

# عرض التغييرات
git diff
```

---

## 📱 استخدام GitHub Desktop

إذا كنت تفضل واجهة رسومية:

1. حمّل [GitHub Desktop](https://desktop.github.com)
2. سجّل دخول بحسابك
3. اختر **Clone Repository**
4. حدد `bndrjj/dash`
5. استخدم الواجهة لإدارة التعديلات

---

## 🎯 نصائح احترافية

✅ **اكتب رسائل commit واضحة وبالعربية**  
مثال: `"إضافة: صفحة الإحصائيات الشهرية"`

✅ **اعمل commit بشكل منتظم**  
لا تنتظر حتى تتراكم التعديلات

✅ **استخدم الفروع للمميزات الكبيرة**  
افصل التطوير عن الإنتاج

✅ **اكتب README.md شامل**  
وثّق مشروعك بوضوح

✅ **استخدم .gitignore بشكل صحيح**  
لا ترفع ملفات غير ضرورية

---

## 🆘 حل المشاكل الشائعة

### المشكلة: رفض GitHub التعديلات

```bash
# سبب: الفرع المحلي قديم
# الحل:
git pull origin main --rebase
git push origin main
```

### المشكلة: نسيت إضافة ملف في آخر commit

```bash
# إضافة الملف
git add forgotten-file.txt

# تعديل آخر commit
git commit --amend --no-edit
```

### المشكلة: رفعت ملف حساس عن طريق الخطأ

```bash
# حذف من Git
git rm --cached sensitive-file.txt

# إضافة للـ .gitignore
echo "sensitive-file.txt" >> .gitignore

# commit الحذف
git commit -m "حذف ملف حساس"
git push origin main
```

---

## 📚 موارد إضافية

- 📖 [توثيق Git الرسمي](https://git-scm.com/doc)
- 📖 [دليل GitHub](https://docs.github.com)
- 🎓 [تعلم Git تفاعلياً](https://learngitbranching.js.org/)
- 📺 [دروس Git بالعربية](https://www.youtube.com/results?search_query=git+tutorial+arabic)

---

## ✅ قائمة المراجعة

قبل رفع مشروعك:

- [ ] تأكد من وجود README.md شامل
- [ ] أضف ملف .gitignore مناسب
- [ ] احذف أي ملفات حساسة
- [ ] اكتب رسائل commit واضحة
- [ ] اختبر المشروع محلياً
- [ ] تأكد من عمل جميع الروابط
- [ ] أضف LICENSE إذا لزم الأمر

---

<div dir="ltr" align="center">

**Happy Coding! 💻✨**

</div>