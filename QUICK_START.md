# ⚡ دليل البدء السريع - لوحة التحكم

## 🎯 خطوات سريعة للبدء

### 1. تحميل المشروع
```bash
git clone https://github.com/bndrjj/dash.git
cd dash
```

### 2. تشغيل اللوحة

**الطريقة الأولى - Python:**
```bash
python -m http.server 8000
```
ثم افتح: `http://localhost:8000`

**الطريقة الثانية - Node.js:**
```bash
npx http-server
```

**الطريقة الثالثة - PHP:**
```bash
php -S localhost:8000
```

### 3. تعديل البيانات

افتح ملف `data.json` في أي محرر نصوص وعدّل البيانات:

```json
{
  "header": {
    "title": "عنوانك هنا",
    "subtitle": "وصفك هنا",
    "date": "التاريخ"
  },
  "stats": [
    {
      "icon": "📊",
      "value": "القيمة",
      "label": "الوصف",
      "color": "default"
    }
  ]
}
```

---

## 🎨 تعديل سريع للألوان

افتح `index.html` وابحث عن `:root` وعدّل:

```css
--accent: #F7B731;       /* اللون المميز */
--accent-2: #FF6B6B;     /* أحمر */
--accent-3: #4ECDC4;     /* فيروزي */
```

---

## 📌 نصائح مهمة

✅ احفظ التغييرات قبل تحديث المتصفح  
✅ استخدم Ctrl+Shift+R لتحديث كامل  
✅ تأكد من صحة JSON قبل الحفظ  
✅ استخدم خادم محلي لأفضل أداء  

---

## 🆘 حل سريع للمشاكل

**المشكلة:** البيانات لا تظهر  
**الحل:** استخدم خادم محلي بدلاً من فتح الملف مباشرة

**المشكلة:** خطأ في JSON  
**الحل:** تحقق من [JSONLint](https://jsonlint.com)

---

للمزيد من التفاصيل، راجع **README.md** 📖